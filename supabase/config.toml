[functions.submit-job]
verify_jwt = false
# Function validates the publishable app key; customer submissions are anonymous.
# Rate limits are enforced in SQL. Never grant anonymous execute on submit_job.
